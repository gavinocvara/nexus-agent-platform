{
  "schema_version": 1,
  "id": "orders_unavailable",
  "title": "Orders service unavailable",
  "description": "Orders remains running but rejects normal health and order traffic.",
  "target": {"service": "orders"},
  "fault": {"type": "service_unavailable"},
  "expected_root_cause": {
    "component": "orders",
    "failure": "request handling unavailable"
  },
  "expected_symptoms": [
    {
      "description": "Gateway order creation fails",
      "via": "gateway",
      "method": "POST",
      "path": "/orders",
      "expected_status": 503,
      "request_body": {"user_id": 1, "item": "scenario-probe", "quantity": 1}
    }
  ],
  "expected_unaffected_components": ["gateway process", "users", "postgres"],
  "activation": {"controller_service": "orders", "failure_id": "orders_unavailable"},
  "recovery": {"reset_required": true, "health_service": "orders", "expected_status": 200},
  "tags": ["orders", "availability", "downstream"],
  "difficulty": "basic"
}
