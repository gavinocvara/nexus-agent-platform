global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: nexus-services
    metrics_path: /metrics
    static_configs:
      - targets:
          - gateway:8000
          - users:8000
          - orders:8000
